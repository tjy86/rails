<%= form_for(@god) do |f| %>
  <% if @god.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@god.errors.count, "error") %> prohibited this god from being saved:</h2>

      <ul>
      <% @god.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

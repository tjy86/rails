<% @movies.each do |movie| %>
<%= image_tag movie.poster %>
<ul>
	<li><%= movie.name %></li>
	<li>Revenue: $<%= movie.gross %> million</li>
	<li><%= movie.actors %></li>
	<li><%= link_to "Delete Movie", delete_movie_path(movie), :method => :delete %></li>
</ul>

<% end%>